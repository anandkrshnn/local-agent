apiVersion: v1
kind: Service
metadata:
  name: local-agent-service
spec:
  selector:
    app: local-agent
  ports:
  - name: dashboard
    protocol: TCP
    port: 8000
    targetPort: 8000
  - name: ollama-proxy
    protocol: TCP
    port: 11434
    targetPort: 11434
  type: LoadBalancer
